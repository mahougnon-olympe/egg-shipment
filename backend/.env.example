PORT=5000
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/egg-orders
JWT_SECRET=remplace_par_une_cle_secrete_longue
JWT_EXPIRES_IN=30d
CLIENT_ORIGINS=http://localhost:3000,http://localhost:3001
# Optionnel — coller le JSON du compte de service Firebase (sur une seule ligne)
FIREBASE_SERVICE_ACCOUNT=
